<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{title}}</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    h5 {
      font-size: 0.75rem;
    }

    p {
      font-size: 0.6rem;
    }

    img {
      width: 100%;
      height: 100%;
    }

    .wrapper {
      padding: 1rem 3rem;
    }

    .signature-img {
      width: 200px;
      height: 100px;
    }

    .banner {
        width: 100%;
        height: 50px;
        background-color: rgb(255, 136, 0);
        color: white;
    }

    .banner .title {
        margin-left: 100px;
    }

    .content {
      min-height: 700px;
    }
  </style>
  <body>
    <div class="banner">
        <div class="title">
            <h2>omega</h2>
        </div>
    </div>
    <div class="wrapper">
      <div>
        <div class="row">
          <div class="col-8">
            <div class="d-flex gap-2">
              <h5>PACIENTE:</h5>
              <p>{{patientFullname}}</p>
            </div>
            <div class="d-flex gap-2">
              <h5>EDAD:</h5>
              <p>{{patientAge}} años</p>
            </div>
            <div class="d-flex gap-2">
              <h5>FECHA:</h5>
              <p>{{date}}</p>
            </div>
            <div class="d-flex gap-2">
              <h5>INSTITUCIÓN:</h5>
              <p>{{company}}</p>
            </div>
          </div>
          <div class="col-4">
            <div class="d-flex gap-2">
              <h5>CEDULA:</h5>
              <p>{{patientDni}}</p>
            </div>
          </div>
        </div>
      </div>
      <div class="d-flex gap-3">
        <h5>ESTUDIO:</h5>
        <p>{{examName}}</p>
      </div>
      <div class="content">{{{content}}}</div>

      <div class="row mt-3">
        <div
          class="col-6 d-flex flex-column align-center justify-content-center"
        >
          <p>Dr/a.{{doctorFullname}}</p>
          <div class="d-flex gap-2">
            <h5>CI:</h5>
            <p>{{doctorDni}}</p>
          </div>
        </div>
        <div class="col-6 d-flex justify-content-end">
          <div class="signature-img">
            <img
              src="{{{doctorSignature}}}"
              alt=""
            />
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
